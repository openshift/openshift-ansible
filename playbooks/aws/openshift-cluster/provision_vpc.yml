---
- hosts: localhost
  connection: local
  gather_facts: no
  tasks:
  - name: create a vpc
    include_role:
      name: openshift_aws
      tasks_from: vpc.yml
    static: true
    when: openshift_aws_create_vpc | default(True) | bool
