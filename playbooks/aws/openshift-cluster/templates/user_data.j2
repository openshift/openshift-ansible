#cloud-config
mounts:
- [ xvdb ]
- [ ephemeral0 ]

write_files:
- content: |
    DEVS=/dev/xvdb
    VG=docker_vg
  path: /etc/sysconfig/docker-storage-setup
  owner: root:root
  permissions: '0644'

{% if deployment_type == 'online' %}
disable_root: 0
{% endif %}
