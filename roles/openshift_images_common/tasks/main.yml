---
- name: Pulling common docker images
  shell: docker pull {{ oic_docker_registry }}/openshift3_beta/{{ item }}:{{ oic_image_version }}
  with_items:
    - ose-deployer
    - ose-sti-builder
    - ose-docker-builder
    - ose-pod
