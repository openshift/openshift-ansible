---
# origin uses community packages named 'origin'
# online currently uses 'atomic-openshift' packages
# openshift-enterprise uses Red Hat packages named 'atomic-openshift'
known_openshift_deployment_types: ['origin', 'online', 'openshift-enterprise']
