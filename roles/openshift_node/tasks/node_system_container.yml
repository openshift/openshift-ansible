---
- name: Pre-pull node system container image
  command: >
    atomic pull --storage=ostree {{ r_openshift_node_system_images_registry }}/{{ r_openshift_node_system_image }}:{{ r_openshift_node_image_tag }}
  register: pull_result
  changed_when: "'Pulling layer' in pull_result.stdout"

- name: Install or Update node system container
  oc_atomic_container:
    name: "{{ r_openshift_node_service_type }}-node"
    image: "{{ r_openshift_node_system_images_registry }}/{{ r_openshift_node_system_image }}:{{ r_openshift_node_image_tag }}"
    state: latest
