---
- name: Pull etcd system container
  command: atomic pull --storage=ostree {{ openshift.etcd.etcd_image }}
  register: pull_result
  changed_when: "'Pulling layer' in pull_result.stdout"

- name: Set initial Etcd cluster
  set_fact:
    etcd_initial_cluster: >-
      {% for host in etcd_peers | default([]) -%}
      {% if loop.last -%}
      {{ hostvars[host].etcd_hostname }}={{ r_etcd_common_peer_url_scheme }}://{{ hostvars[host].etcd_ip }}:{{ r_etcd_common_peer_port }}
      {%- else -%}
      {{ hostvars[host].etcd_hostname }}={{ r_etcd_common_peer_url_scheme }}://{{ hostvars[host].etcd_ip }}:{{ r_etcd_common_peer_port }},
      {%- endif -%}
      {% endfor -%}

- name: Install or Update Etcd system container package
  oc_atomic_container:
    name: etcd
    image: "{{ openshift.etcd.etcd_image }}"
    state: latest
    values:
      - ETCD_LISTEN_PEER_URLS={{ r_etcd_listen_peer_urls }}
      - ETCD_NAME={{ etcd_hostname }}
      - ETCD_INITIAL_CLUSTER={{ etcd_initial_cluster }}
      - ETCD_LISTEN_CLIENT_URLS={{ r_etcd_listen_client_urls }}
      - ETCD_INITIAL_ADVERTISE_PEER_URLS={{ r_etcd_initial_advertise_peer_urls }}
      - ETCD_INITIAL_CLUSTER_STATE={{ r_etcd_initial_cluster_state }}
      - ETCD_INITIAL_CLUSTER_TOKEN={{ r_etcd_initial_cluster_token }}
      - ETCD_ADVERTISE_CLIENT_URLS={{ r_etcd_advertise_client_urls }}
      - ETCD_CA_FILE={{ r_etcd_common_system_container_conf_dir }}/ca.crt
      - ETCD_CERT_FILE={{ r_etcd_common_system_container_conf_dir }}/server.crt
      - ETCD_KEY_FILE={{ r_etcd_common_system_container_conf_dir }}/server.key
      - ETCD_PEER_CA_FILE={{ r_etcd_common_system_container_conf_dir }}/ca.crt
      - ETCD_PEER_CERT_FILE={{ r_etcd_common_system_container_conf_dir }}/peer.crt
      - ETCD_PEER_KEY_FILE={{ r_etcd_common_system_container_conf_dir }}/peer.key
