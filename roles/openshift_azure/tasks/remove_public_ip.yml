---
- name: remove public ip
  azure_rm_publicipaddress:
    resource_group: "{{ item.resource_group }}"
    name: "{{ openshift_azure_prefix }}-{{ item.name }}"
    state: absent
  with_items: "{{ openshift_azure_public_ips }}"
