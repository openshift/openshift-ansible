---
network_file: /etc/sysconfig/network-scripts/ifcfg-eth0
dns_domain: "{{ lookup('env', 'CLOUD_DOMAIN')|default('example.com', true) }}"
        # FIXME get the DNS server IP address,
dns_server: "{{ hostvars['dns.example.com'].openshift.common.ip }}"
named_dir: /var/named
forwarders:
  - 10.5.30.45
  - 10.5.30.46
zone_serial: "2015062401"
