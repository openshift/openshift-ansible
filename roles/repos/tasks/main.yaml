---
# The following role lays down the correct repository and gpg key for yum
- name: Create Devel Repos
  template:
    src: repo.j2
    dest: "/etc/yum.repos.d/{{ item.key }}.repo"
  with_dict: oo_devel_repos
