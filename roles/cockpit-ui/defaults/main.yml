---
openshift_config_base: "/etc/origin"
openshift_master_config_dir: "{{ openshift.common.config_base | default(openshift_config_base) }}/master"
openshift_cockpit_docker_registry_hostname: "docker-registry-default.{{ openshift_master_default_subdomain }}"
openshift_cockpit_registry_console_hostname: "registry-console-default.{{ openshift_master_default_subdomain }}"
