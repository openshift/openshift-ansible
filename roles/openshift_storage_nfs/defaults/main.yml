---
openshift:
  hosted:
    registry:
      storage:
        nfs:
          directory: "/srv/exports"
          options: "*(rw,root_squash)"
        volume:
          name: "registry"
    metrics:
      deploy: False
      storage:
        nfs:
          directory: "/srv/exports"
          options: "*(rw,root_squash)"
        volume:
          name: "metrics"
