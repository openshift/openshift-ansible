---
#
# These vars are generally considered private and not expected to be altered
# by end users
#

# Adding nfs so that we can interpret it later as 'pv'
openshift_metrics_cassandra_storage_types:
- emptydir
- pv
- dynamic
- nfs
