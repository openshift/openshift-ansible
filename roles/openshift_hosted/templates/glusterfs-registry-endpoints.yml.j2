---
apiVersion: v1
kind: Endpoints
metadata:
  name: glusterfs-{{ glusterfs_name }}-endpoints
subsets:
- addresses:
{% for ep in openshift_hosted_registry_glusterfs_endpoints %}
  - ip: {{ ep }}
{% endfor %}
  ports:
  - port: 1
