apiVersion: image.openshift.io/v1
kind: ImageStreamTag
metadata:
  name: {{ node_tag_name }}
  namespace: openshift-infra
tag:
  reference: true
  from:
    kind: DockerImage
    name: {{ osn_image }}
