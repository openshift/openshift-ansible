---
- name: Provision networking
  include_tasks: provision-networking.yml

- name: Provision the control plane
  include_tasks: provision-ctlplane.yml

- name: Provision nodes
  include_tasks: provision-nodes.yml

- name: Add the new nodes to the inventory
  meta: refresh_inventory
