---
- name: Install master services files
  template:
    src: "{{ item }}.j2"
    dest: "/etc/systemd/system/{{ openshift_service_type }}-{{ item }}"
    owner: root
    group: root
    mode: 600
  with_items:
  - master-api.service
  - master-controllers.service
  - master-etcd.service
  notify:
  - reload systemd units
